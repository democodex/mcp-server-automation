# Method 1: Direct Command (Entrypoint Mode)
# Build MCP servers using direct commands like npx or uvx

build:
  entrypoint:
    command: "npx"
    args: 
      - "-y"
      - "@modelcontextprotocol/server-everything"
  
  # Build locally only
  push_to_ecr: false

deploy:
  enabled: false